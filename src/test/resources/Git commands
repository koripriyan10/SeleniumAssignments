-- Intialize git and add remote URL, add,commit and push changes to remote repository

git remote remove origin
git remote add origin https://github.com/koripriyan10/Selenium_Assignments.git
git add .
git commit -m "Browser Automation"
git push -u origin main

--Create new Feature

git checkout -b feature1


--Deleting file which present from local repo
git rm -r <Foldername>

--Restoring Deleted file which is not commited yet  from local repo
git checkout HEAD  <Foldername>

--Deleting a File only from remote repo but not from local and commiting and pushing changes.

$ git rm --cached -r BrowserAutomation
$ git commit -m "tests"
$ git remote add origin <Url name>
$ git push -u origin main
